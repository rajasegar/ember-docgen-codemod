{
  "name": "ember-docgen-codemod",
  "version": "0.1.12",
  "scripts": {
    "test": "codemod-cli test",
    "update-docs": "codemod-cli update-docs",
    "deploy": "npm version patch && git push && git push --tags && npm publish"
  },
  "bin": "./bin/cli.js",
  "keywords": [
    "codemod-cli"
  ],
  "dependencies": {
    "codemod-cli": "^0.2.10"
  },
  "devDependencies": {
    "jest": "^24.1.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
